int ft_is_prime(int nb)
{
    
}

#include <stdio.h>
int main(void)
{
    int nb1 = 5;
    int nb2 = 4;
    printf("If 1 is prime, 0 if is not\n");
    printf("The num %d is %d\n", nb1, ft_is_prime(nb1));
    printf("The num %d is %d\n", nb2, ft_is_prime(nb2));
}